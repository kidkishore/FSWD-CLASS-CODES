today class is Rnadum user generator api creation in using html, css, javascript

asyncronos function as providing a promis promis------->pending, resolve, reject.

edhannul mattumdhan await engira function use seiya mudiyum.


 <h1>Randum User Generator</h1>
<!-- it is used to store the randum user generated data -->
            <div id="user"> </div>
<!-- it is used to store the randum user generated data -->
        <button onclick = "fetchData()">Get User</button>
    </div>
    <script>
        let userContainer = document.getElementById('user').value;
        async function fetchData() {
         try{
            // first step fetch the apidata in the following api code source link
            let response = await fetch('https://randomuser.me/api/');
            //wait the data was fetchig process still ending.
            let data = await response.json();
            //convert the fetched data to javascript code
            let userData = data.result[0];
            // give the first user data in the api data
            let name = `${userData.name.tittle} ${userData.name.first} ${userData.name.last}`//userData.name.first;
            // get the user datas first detail
            userContainer.innerHTML = name;
            }  